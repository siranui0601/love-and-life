import * as current from './battle-simulator-checkpoint-c-extended-base.mjs';
import { applyEquipmentRoundRuntime, initializeEquipmentBattleRuntime, prepareEquipmentSkill } from './player-equipment-runtime.mjs';

export * from './battle-simulator-checkpoint-c-extended-base.mjs';

const PUBLIC_REASON=Object.freeze({no_owned_field:'field_required',no_repeatable_history:'missing_history',uses_exhausted:'use_limit',weapon_requirement:'wrong_weapon'});const publicReason=(reason)=>PUBLIC_REASON[reason]??reason;
function transformedData(data,session){let playerSkillById=null;const blockedReasons=new Map();for(const[id,skill]of data.playerSkillById){const prepared=prepareEquipmentSkill({skill,session});if(prepared.blockedReason)blockedReasons.set(id,prepared.blockedReason);if(prepared.skill===skill)continue;playerSkillById??=new Map(data.playerSkillById);playerSkillById.set(id,prepared.skill);}if(!playerSkillById)return{data,blockedReasons};return{data:{...data,playerSkillById,playerSkills:(data.playerSkills??[]).map((skill)=>playerSkillById.get(skill.id)??skill)},blockedReasons};}
function skillIdFromActionId(actionId){return String(actionId??'').match(/^SKILL:(SKL-\d{4})(?::|$)/u)?.[1]??null;}
function syncResult(output){if(!output?.session||!output.result)return;const state=output.session.state;output.result.players=state.players.map((actor)=>({id:actor.id,hp:actor.hp,maxHp:actor.maxHp,mp:actor.mp,maxMp:actor.maxMp,alive:actor.alive,mpSpent:actor.mpSpent,damageDealt:actor.damageDealt}));output.result.enemies=state.enemies.map((actor)=>({id:actor.id,hp:actor.hp,maxHp:actor.maxHp,alive:actor.alive,escaped:actor.escaped}));output.result.playerRuntimeMechanics=structuredClone(output.session.playerRuntimeMechanics);if(output.result.timeline)output.result.timeline.frames=output.session.frames;}
function decorate(command,blockedReason=null){const raw=blockedReason??command.disabledReason;const disabledReason=publicReason(raw);return{...command,available:blockedReason?false:command.available,disabledReason,...(blockedReason?{targets:[]}:{}),reasonCode:disabledReason};}
function staleNoTargetCommand(session){const playerAlive=(session.state?.players??[]).some((actor)=>actor.alive&&Number(actor.hp??0)>0);const enemyAlive=(session.state?.enemies??[]).some((actor)=>actor.alive&&Number(actor.hp??0)>0&&!actor.escaped);if(session.status==='active'&&playerAlive&&!enemyAlive)return[{actionId:'ATTACK',kind:'attack',name:'こうげき',target:'single_enemy',available:false,disabledReason:'no_target',reasonCode:'no_target',targets:[]}];return null;}
export function beginInteractiveBattle(options){const session=current.beginInteractiveBattle(options);initializeEquipmentBattleRuntime({data:options.data,session});return session;}
export function listInteractiveBattleCommands({data,session}){if(!session)return current.listInteractiveBattleCommands({data,session});const noTarget=staleNoTargetCommand(session);if(noTarget)return noTarget;const transformed=transformedData(data,session);return current.listInteractiveBattleCommands({data:transformed.data,session}).map((command)=>decorate(command,command.skillId?transformed.blockedReasons.get(command.skillId):null));}
export function resolveInteractiveBattleRound({data,session,command}){if(!session)return current.resolveInteractiveBattleRound({data,session,command});const displayed=listInteractiveBattleCommands({data,session}).find((entry)=>entry.actionId===command?.actionId);if(displayed&&!displayed.available)return{ok:false,reason:displayed.disabledReason??'action_unavailable',session};const skillId=skillIdFromActionId(command?.actionId);const transformed=transformedData(data,session);const blockedReason=skillId?transformed.blockedReasons.get(skillId):null;if(blockedReason)return{ok:false,reason:publicReason(blockedReason),session};const output=current.resolveInteractiveBattleRound({data:transformed.data,session,command});if(!output?.ok){if(output?.reason)output.reason=publicReason(output.reason);return output;}if(!output.session)return output;applyEquipmentRoundRuntime({data,session:output.session,round:output.round});if(output.session.status==='active')output.commands=listInteractiveBattleCommands({data,session:output.session});syncResult(output);return output;}
