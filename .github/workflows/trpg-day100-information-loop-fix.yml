name: TRPG authored weapon-shop first visit staging

on:
  workflow_dispatch:

jobs:
  staging:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The reviewed patch is applied by the existing PR integration workflow."
