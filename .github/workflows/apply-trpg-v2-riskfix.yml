name: Apply TRPG v2 encounter selection fix

on:
  push:
    branches:
      - agent/trpg-player-simulation-v2-20260717
    paths:
      - ".github/workflows/apply-trpg-v2-riskfix.yml"
      - "tools/trpg-sim/.v2-riskfix/**"

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/trpg-player-simulation-v2-20260717
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: python tools/trpg-sim/.v2-riskfix/apply.py
      - run: node --check tools/trpg-sim/lib/player-journey.mjs
      - run: npm ci
      - run: npm run trpg:test
      - name: Commit encounter selection fix
        run: |
          rm -rf tools/trpg-sim/.v2-riskfix
          rm -f .github/workflows/apply-trpg-v2-riskfix.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "TRPG v2: match ordinary encounter risk to player progression" || exit 0
          git push origin HEAD:agent/trpg-player-simulation-v2-20260717
