name: Apply TRPG authored foundation integration

on:
  push:
    branches:
      - chatgpt/trpg-authored-content-refactor
    paths:
      - ".github/workflows/apply-trpg-authored-foundation.yml"
      - "tools/trpg-refactor/apply-authored-foundation.mjs"

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          ref: chatgpt/trpg-authored-content-refactor
          fetch-depth: 1
          show-progress: false
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Integrate canonical weather and facility visibility
        run: node tools/trpg-refactor/apply-authored-foundation.mjs
      - run: npm ci
      - name: Run complete TRPG regression suite
        run: npm run trpg:test
      - name: Commit integrated source
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "feat(trpg): integrate canonical weather and facility timelines"
          git push origin HEAD:chatgpt/trpg-authored-content-refactor
