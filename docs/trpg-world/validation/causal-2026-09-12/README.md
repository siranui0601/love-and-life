# Versioned causal evidence

The manifest pins each replay to its runtime commit and records normalized artifact hashes. Use a separate checkout at that commit when reproducing a historical record with `tools/trpg-world/validation/replay.mjs`; do not treat the content hash alone as a guarantee across code changes.

- 09679baf mine: actual collapse, local account, conversation, preparation, access work, two survivor recoveries, continued life; full replay and intermediate JSON replay PASS.
- 09679baf fire-first-price-failure: retained failed run, not a success certificate.
- d9e0de19 fire: actual fire, local discovery, extinguishing, changed-price reinspection, repair and reopening; historical failure remains. Exact replay PASS.
- d9e0de19 prevention: physical fuel cleanup, then independent prevention evaluation; exact replay PASS.

These multi-day records used fixed work/meal/rest cycles. Their daytime bedtimes disqualify them as natural-life acceptance evidence. They remain valid physical-causality and regression stress traces. The separate ordinary mode is being validated using public hunger, fatigue and night-time conditions.

The mine disk-restoration check passed before the final physical-description correction. Applying the newer runtime to that older record diverges at operation 2700 because NPC089 now sees repair marks rather than unrepaired wreckage. The isolated pinned runtime still matches the old hash at that operation; the only differing state leaf is recorded in cross-runtime-difference.json. Do not relabel that cross-version diagnostic as save corruption or as a successful current-version full replay.

All full state, event identifiers and internal fields in these files are developer-only evidence. The journey has an explicitly structural inter-region travel prefix; it is not a fully blind world-discovery certificate. Browser/human playability and arbitrary HTTP time partition equivalence are not established by these artifacts. Paid/live narrative calls: 0.
