const canvas = document.querySelector('#gameCanvas');
const ctx = canvas.getContext('2d');
const $ = (selector) => document.querySelector(selector);

const els = {
  venue: $('#venueScreen'), gen: $('#generateScreen'), nav: $('#bottomNav'), kick: $('#kickBtn'), center: $('#centerBtn'), generate: $('#generateBtn'), dock: $('#gimmickDock'), dockList: $('#dockList'), fieldHelp: $('#fieldHelp'), coachTitle: $('#coachTitle'), coachText: $('#coachText'), round: $('#roundLabel'), goal: $('#goalLabel'), emojiCount: $('#emojiCountLabel'), emojiGrid: $('#emojiGrid'), selectedRow: $('#selectedRow'), modal: $('#modal'), modalKicker: $('#modalKicker'), modalTitle: $('#modalTitle'), modalText: $('#modalText'), modalPrimary: $('#modalPrimary'), modalSecondary: $('#modalSecondary'), toast: $('#toast'),
};

const WORLD = { w: 900, gravity: 0.36, wallBounce: 0.78 };
const START = { x: 450, y: 165 };
const FALLBACK_EMOJIS = ['👻','🦀','🚀','💪🏾','🧱','🥝','🧲','🏘️','🐸','🎈','🍌','🌪️','🚪','🛞','🧊','☁️','⚡','🕳️','🪜','🧵','⚙️','🛝','🦘','🪭','🪨','🔘'];
let emojiPool = [...FALLBACK_EMOJIS];

const MOTOR_META = {
  launcher: { label: '発射', icon: '🚀', color: '#ffbd59', short: '最低速度つきで飛ばす' },
  bumper: { label: '反発', icon: '💥', color: '#e8ff59', short: '衝突角で跳ね返す' },
  fieldForce: { label: '力場', icon: '🌪️', color: '#76e4ff', short: '範囲内に力を加える' },
  gravityShift: { label: '重力角度', icon: '🔄', color: '#ff8ad1', short: '重力方向を変える' },
  split: { label: '分裂', icon: '🧬', color: '#b28cff', short: 'ボールを分身させる' },
  portal: { label: 'ワープ', icon: '🚪', color: '#c8a7ff', short: '別地点へ移す' },
  rail: { label: 'レール', icon: '🛤️', color: '#ffffff', short: '線上に沿わせる' },
  carrier: { label: '運搬', icon: '☁️', color: '#ffdf70', short: '動く足場で運ぶ' },
  flipper: { label: 'フリッパー', icon: '🦾', color: '#9cffc1', short: '動く腕で叩く' },
  phase: { label: '透過', icon: '👻', color: '#c8d8ff', short: '一時的に衝突を変える' },
  dragZone: { label: '抵抗場', icon: '🧊', color: '#b7d7ff', short: '速度を整えて出口へ流す' },
  timerRelease: { label: '捕球放出', icon: '🧤', color: '#ffbd59', short: '捕まえて時間差で放つ' },
};
const MOTOR_KINDS = Object.keys(MOTOR_META);
const BODY_SHAPES = ['point','line','area','fan','gate','rail','carrier','arm','platform'];
const BODY_MOTIONS = ['none','spin','slide','bob','swing','orbit','pendulum'];
const LEGACY_KIND_MAP = { kick:'launcher', cannon:'launcher', spring:'launcher', wind:'fieldForce', updraft:'fieldForce', vortex:'fieldForce', current:'fieldForce', antiFall:'fieldForce', redirect:'launcher', mirror:'bumper', curve:'fieldForce', oneWay:'rail', tether:'fieldForce', orbit:'fieldForce', catchRelease:'timerRelease', pendulum:'flipper', gravityFlip:'gravityShift', slow:'dragZone', speedFloor:'fieldForce' };

let state = freshState();
let drag = null;
let modalAction = null;
let toastTimer = null;
let ballSerial = 1;

function freshState() { return { tab: 'venue', tutorial: 'intro', phase: 'ready', round: 1, cameraY: 0, runTime: 0, balls: [makeBall({ main: true })], goals: [{ id: 1, x: 690, y: 360, w: 116, h: 78, done: false, label: 'A' }], ownGoals: [], fieldEmojis: [], inventory: [], selected: [], gimmicks: [], placing: null, focusGimmick: null, nextGimmick: 1, nextGoal: 2, generateUnlocked: false, tabUnlocked: false, gameover: false, last: 0 }; }
function makeBall(options = {}) { return { id: options.id || `b${ballSerial++}`, main: Boolean(options.main), generation: options.generation || 0, x: options.x ?? START.x, y: options.y ?? START.y, vx: options.vx || 0, vy: options.vy || 0, r: options.r || (options.main ? 18 : 15), expiresAt: options.expiresAt ?? Infinity, phaseUntil: 0, gravityUntil: 0, gravityAngle: 90, gravityStrength: 1 }; }
function mainBall() { return state.balls.find((ball) => ball.main) || state.balls[0]; }
function uid() { return `${Date.now().toString(36)}-${Math.random().toString(36).slice(2)}`; }
function hash(str) { let h = 2166136261; for (let i = 0; i < str.length; i += 1) { h ^= str.charCodeAt(i); h = Math.imul(h, 16777619); } return h >>> 0; }
function pick(arr, n) { return arr[Math.abs(Number(n) || 0) % arr.length]; }
function clamp(v, min, max) { return Math.max(min, Math.min(max, v)); }
function toast(msg) { els.toast.textContent = msg; els.toast.hidden = false; clearTimeout(toastTimer); toastTimer = setTimeout(() => { els.toast.hidden = true; }, 1800); }
function setCoach(title, text) { els.coachTitle.textContent = title; els.coachText.textContent = text; }
function showHelp(text) { els.fieldHelp.textContent = text; els.fieldHelp.hidden = false; }
function hideHelp() { els.fieldHelp.hidden = true; }
function showModal(kicker, title, text, primary, action, secondary) { els.modalKicker.textContent = kicker; els.modalTitle.textContent = title; els.modalText.textContent = text; els.modalPrimary.textContent = primary; els.modalSecondary.hidden = !secondary; els.modalSecondary.textContent = secondary || ''; modalAction = action; els.modal.classList.add('is-open'); }
function closeModal() { els.modal.classList.remove('is-open'); }
async function loadEmojiCatalog() { try { const res = await fetch('./emoji_catalog_full_ja.json', { cache: 'force-cache' }); if (!res.ok) return; const data = await res.json(); const list = Array.isArray(data) ? data : Object.values(data).flat(); const normalized = list.map((item) => (typeof item === 'string' ? item : (item.emoji || item.char || item.symbol || item.unicode || ''))).filter(Boolean); if (normalized.length > 30) emojiPool = normalized; } catch (_) {} }
async function boot() { await loadEmojiCatalog(); grantEmojis(8); render(); fitCameraToGoals(); showModal('KICK OFF', '今宵はサッカー！', 'ボールを黄色いゴールへ入れるゲームです。まずは会場を見て、下のキックオフを押そう⚽️', '会場を見る', () => { closeModal(); showHelp('下の「キックオフ」を押すとボールが動きます。黄色いGOAL Aへ入れよう。'); }); requestAnimationFrame(loop); }
function grantEmojis(n) { for (let i = 0; i < n; i += 1) state.inventory.push({ id: uid(), emoji: pick(emojiPool, hash(`${Date.now()}-${i}-${state.inventory.length}`)) }); }
function spawnFieldEmojis(n) { const base = hash(`field-${state.round}-${state.fieldEmojis.length}`); for (let i = 0; i < n; i += 1) state.fieldEmojis.push({ id: uid(), emoji: pick(emojiPool, base + i * 37), x: 110 + ((base + i * 91) % 680), y: 250 + ((base + i * 141) % 640), r: 20 }); }
function setTab(tab) { if (tab === 'generate' && !state.generateUnlocked) { toast('まずキックオフして、細工を解禁しよう'); return; } state.tab = tab; els.venue.classList.toggle('is-active', tab === 'venue'); els.gen.classList.toggle('is-active', tab === 'generate'); document.querySelectorAll('.tab-btn').forEach((button) => button.classList.toggle('is-active', button.dataset.tab === tab)); render(); if (tab === 'venue') fitCameraToGoals(); }
function startRun() { if (state.phase === 'run' || state.phase === 'between') return; state.gameover = false; resetGimmicksToHome(); state.phase = 'run'; state.runTime = 0; state.balls = [makeBall({ main: true })]; setCoach('キックオフ中', 'この1回のキックオフで、黄色いゴールを全部くぐろう。'); hideHelp(); render(); fitCameraToGoals(); }
function trialReset(title, text) { state.phase = 'edit'; state.gameover = false; state.runTime = 0; state.balls = [makeBall({ main: true })]; state.goals.forEach((goal) => { goal.done = false; }); resetGimmicksToHome(); setCoach(title || '作戦タイム', text || 'ゴールは復活しました。次のキックオフで全ゴール通過を狙おう。'); render(); fitCameraToGoals(); }
function resetAll() { state = freshState(); grantEmojis(8); render(); fitCameraToGoals(); showModal('KICK OFF', '今宵はサッカー！', 'ボールを黄色いゴールへ入れるゲームです。まずは会場を見て、下のキックオフを押そう⚽️', '会場を見る', () => { closeModal(); showHelp('下の「キックオフ」を押すとボールが動きます。黄色いGOAL Aへ入れよう。'); }); }
function firstFallTutorial() { state.generateUnlocked = true; state.tabUnlocked = true; els.nav.hidden = false; trialReset('落下！このままだと届かない', '下の「生成」タブで絵文字を3つ選び、ボールを助けるギミックを作ろう。'); showModal('作戦タイム', 'そうだ、細工をしよう！', 'このままだとボールは落ちます。生成タブで絵文字を3つ選んで、ゴールへ導くギミックを作ろう。', '生成タブへ', () => { closeModal(); setTab('generate'); }); state.tutorial = 'generate'; }
function ownGoalReset() { showModal('OWN GOAL', 'オウンゴール！', 'その試走は失敗扱い。分身のオウンゴールでもアウトです。盤面はそのまま作戦タイムへ戻ります。', '作戦タイムへ', () => { closeModal(); trialReset('オウンゴール。作戦タイム', '盤面は維持。次のキックオフで全ゴール通過を狙おう。'); }); }
async function generateGimmick() { if (state.selected.length !== 3) { toast('絵文字を3つ選んで！'); return; } const recipe = state.selected.map((item) => item.emoji); const seed = hash(recipe.join('|')); const fallback = buildLocalGimmick(recipe, seed); const generated = await requestGeminiGimmick(recipe, fallback); const gimmick = makeGimmickFromData({ ...fallback, ...generated }, recipe, seed); state.nextGimmick += 1; state.gimmicks.push(gimmick); for (const item of state.selected) { const index = state.inventory.findIndex((owned) => owned.id === item.id); if (index >= 0) state.inventory.splice(index, 1); } state.selected = []; state.placing = gimmick.id; state.focusGimmick = gimmick.id; setTab('venue'); setCoach('ギミックを設置しよう', `${gimmick.visualLabel}：${gimmick.shortEffect}`); showHelp(`「${gimmick.name}」を設置できます。下のカードを押したままコートへドラッグ。`); if (state.tutorial === 'generate') { showModal('配置しよう', '会場へ戻った！', '下のギミックカードを押したまま、コートへドラッグして離そう。設置後は本体ドラッグで移動できます。', '配置する', () => closeModal()); state.tutorial = 'place'; } render(); fitCameraToGoals(); }
function makeGimmickFromData(data, recipe, seed) { const motors = normalizeMotors(data.motors || data.effects || []); const body = normalizeBody(data.body || { shape: data.shape }, motors); const x = 450; const y = 520; const angle = (seed % 360) * Math.PI / 180; return { id: `g${state.nextGimmick}`, recipe, name: String(data.name || `審議中${recipe.join('')}`).slice(0, 24), flavor: String(data.flavor || `${recipe.join('')}から生まれたボール細工。`).slice(0, 80), visualLabel: String(data.visualLabel || '装置').slice(0, 12), shortEffect: String(data.shortEffect || summarizeMotors(motors)).slice(0, 44), body, motors, exit: normalizeExit(data.exit, motors), x, y, homeX: x, homeY: y, angle, homeAngle: angle, placed: false, runtime: makeRuntime() }; }
function makeRuntime() { return { t: 0, cooldowns: {}, inside: {}, holding: {}, lastX: 0, lastY: 0, dx: 0, dy: 0 }; }
function resetGimmicksToHome() { state.gimmicks.forEach((g) => resetGimmickToHome(g)); }
function resetGimmickToHome(g) { g.x = g.homeX ?? g.x; g.y = g.homeY ?? g.y; g.angle = g.homeAngle ?? g.angle; g.runtime = makeRuntime(); g.runtime.lastX = g.x; g.runtime.lastY = g.y; }
function saveGimmickHome(g) { g.homeX = g.x; g.homeY = g.y; g.homeAngle = g.angle; }
function buildLocalGimmick(recipe, seed) { const kinds = ['launcher','fieldForce','split','gravityShift','bumper','portal','flipper','rail','carrier','timerRelease']; const first = pick(kinds, seed); const second = pick(kinds.filter((kind) => kind !== first), seed >>> 5); const motors = normalizeMotors([{ kind: first, trigger: 'contact', direction: 'angle', power: 0.78, range: 0.58, duration: 0.5, angle: 180, count: 2 }, { kind: second, trigger: 'inside', direction: 'towardNextGoal', power: 0.55, range: 0.62, duration: 0.8, angle: 90, count: 2 }]); return { name: `審議中${recipe.join('')}`, flavor: `${recipe.join('')}から生まれたボール細工。`, visualLabel: pick(['装置', '細工', '仕掛け', 'ゲート', '足場', '分裂', '重力'], seed >>> 8), shortEffect: summarizeMotors(motors), body: { shape: defaultShapeForMotors(motors), solid: shouldBeSolid(defaultShapeForMotors(motors), motors), size: 0.68, motion: defaultMotionForMotors(motors), motionPower: 0.65 }, motors, exit: { direction: 'towardNextGoal', minSpeed: 7.2, afterSeconds: 0.7 } }; }
async function requestGeminiGimmick(recipe, fallback) { try { const response = await fetch('/api/nohand-soccer/gimmick', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ emojis: recipe }) }); if (!response.ok) throw new Error(`HTTP ${response.status}`); return await response.json(); } catch (error) { console.warn('[noHand] Gemini gimmick fallback', error); toast('AI生成が混雑中。ローカル生成で続行します。'); return fallback; } }
function normalizeMotors(motors) { const source = Array.isArray(motors) ? motors : []; const normalized = source.slice(0, 5).map((motor) => { const kind = normalizeMotorKind(motor?.kind); return { kind, trigger: normalizeOne(motor?.trigger, ['contact','inside','enter','periodic','timer'], defaultTrigger(kind)), direction: normalizeOne(motor?.direction, ['angle','up','down','left','right','radialIn','radialOut','tangent','towardNextGoal','awayFromBall'], 'angle'), power: clamp(Number(motor?.power ?? 0.78), 0.18, 1), range: clamp(Number(motor?.range ?? 0.58), 0.15, 1), duration: clamp(Number(motor?.duration ?? 0.65), 0.05, 3), angle: clamp(Number(motor?.angle ?? 180), -360, 360), count: Math.round(clamp(Number(motor?.count ?? 2), 2, 4)), spreadAngle: clamp(Number(motor?.spreadAngle ?? 38), 10, 140), mode: String(motor?.mode || '') }; }); if (!normalized.length) normalized.push({ kind: 'launcher', trigger: 'contact', direction: 'angle', power: 0.78, range: 0.55, duration: 0.45, angle: 180, count: 2, spreadAngle: 38, mode: '' }); if (normalized.length === 1) normalized.push({ kind: needsPath(normalized[0].kind) ? 'launcher' : 'fieldForce', trigger: 'inside', direction: 'towardNextGoal', power: 0.58, range: 0.65, duration: 0.8, angle: 0, count: 2, spreadAngle: 38, mode: 'exit' }); if (!normalized.some((m) => ['launcher','split','gravityShift','portal','flipper','timerRelease','bumper'].includes(m.kind))) normalized.push({ kind: 'launcher', trigger: 'contact', direction: 'angle', power: 0.72, range: 0.5, duration: 0.45, angle: 0, count: 2, spreadAngle: 38, mode: 'assist' }); return normalized.slice(0, 5); }
function normalizeMotorKind(kind) { const raw = String(kind || '').toLowerCase(); if (MOTOR_KINDS.includes(raw)) return raw; if (LEGACY_KIND_MAP[raw]) return LEGACY_KIND_MAP[raw]; if (/split|clone|分裂|分身|複製/.test(raw)) return 'split'; if (/gravity|重力/.test(raw)) return 'gravityShift'; if (/portal|warp|door|ワープ/.test(raw)) return 'portal'; if (/rail|track|レール/.test(raw)) return 'rail'; if (/carrier|lift|move|運|台車/.test(raw)) return 'carrier'; if (/flipper|arm|pendulum|振り子|腕|叩/.test(raw)) return 'flipper'; if (/phase|ghost|pass|透/.test(raw)) return 'phase'; if (/slow|drag|brake|減速|抵抗/.test(raw)) return 'dragZone'; if (/catch|hold|release|掴|捕|放/.test(raw)) return 'timerRelease'; if (/bumper|mirror|bounce|反射|反発/.test(raw)) return 'bumper'; if (/force|wind|flow|vortex|tether|風|流|渦|引/.test(raw)) return 'fieldForce'; return 'launcher'; }
function normalizeOne(value, allowed, fallback) { const raw = String(value || '').trim(); return allowed.includes(raw) ? raw : fallback; }
function defaultTrigger(kind) { return ['fieldForce','gravityShift','dragZone','carrier','rail'].includes(kind) ? 'inside' : 'contact'; }
function normalizeBody(body, motors) { const inferred = defaultShapeForMotors(motors); let shape = normalizeOne(body?.shape, BODY_SHAPES, inferred); const motion = normalizeOne(body?.motion, BODY_MOTIONS, body?.motion || defaultMotionForMotors(motors)); return { shape, solid: Boolean(body?.solid ?? shouldBeSolid(shape, motors)), size: clamp(Number(body?.size ?? 0.65), 0.35, 1), motion, motionPower: clamp(Number(body?.motionPower ?? 0.55), 0, 1) }; }
function normalizeExit(exit, motors) { const risky = motors.some((m) => needsPath(m.kind)); if (!risky && exit == null) return null; return { direction: normalizeOne(exit?.direction, ['angle','towardNextGoal','radialOut','up','left','right'], 'towardNextGoal'), minSpeed: clamp(Number(exit?.minSpeed ?? 7.2), 5.8, 11), afterSeconds: clamp(Number(exit?.afterSeconds ?? 0.75), 0.25, 1.8), label: String(exit?.label || '出口補助').slice(0, 16) }; }
function needsPath(kind) { return ['fieldForce','gravityShift','dragZone','rail','carrier','timerRelease'].includes(kind); }
function defaultShapeForMotors(motors) { if (motors.some((m) => m.kind === 'rail')) return 'rail'; if (motors.some((m) => m.kind === 'carrier')) return 'carrier'; if (motors.some((m) => m.kind === 'flipper')) return 'arm'; if (motors.some((m) => ['portal','phase'].includes(m.kind))) return 'gate'; if (motors.some((m) => ['fieldForce','gravityShift','dragZone','split'].includes(m.kind))) return 'fan'; return 'point'; }
function defaultMotionForMotors(motors) { if (motors.some((m) => m.kind === 'flipper')) return 'pendulum'; if (motors.some((m) => m.kind === 'carrier')) return 'slide'; if (motors.some((m) => m.kind === 'fieldForce' && ['tangent','radialIn'].includes(m.direction))) return 'spin'; return 'none'; }
function shouldBeSolid(shape, motors) { return ['rail','platform','line','carrier','arm'].includes(shape) || motors.some((m) => ['bumper','rail','carrier','flipper'].includes(m.kind)); }
function summarizeMotors(motors) { return motors.slice(0, 3).map((m) => MOTOR_META[m.kind]?.short || '動かす').join('＋'); }
function roundClearSequence() { state.phase = 'between'; resetGimmicksToHome(); render(); showModal('GOOOOL!', 'GOOOOL！', '全ゴール通過！ 次はコートが広がり、新しいゴールが増えます。', '次へ', () => { closeModal(); animateBallHome(() => { state.round += 1; state.goals.forEach((goal) => { goal.done = false; }); const newGoal = addGoal(); render(); focusCameraOn(newGoal.y); setTimeout(() => { fitCameraToGoals(); setTimeout(() => showAfterGoalTutorial(), 450); }, 650); }); }); }
function showAfterGoalTutorial() { showModal('ROUND UP', 'ゴールが増えた！', '大丈夫、ゴールが増えただけだ！素材が足りない……？ 安心してくれ。', '素材を見る', () => { closeModal(); spawnFieldEmojis(5); render(); showModal('素材出現', '絵文字が出現した！', 'ゴールを決めると、コートに絵文字が出ます。ボールで触れると素材回収。素材3つでギミックを作り、全ゴールを完遂するんだ！', '了解', () => { closeModal(); trialReset('全ゴールをくぐろう', '黄色いゴールをすべて通過すると次ラウンド。絵文字はボールで触れると素材になります。'); }); }); }
function animateBallHome(done) { const ball = mainBall(); const start = { x: ball.x, y: ball.y }; const end = { ...START }; const t0 = performance.now(); function step(now) { const t = clamp((now - t0) / 650, 0, 1); const ease = 1 - Math.pow(1 - t, 3); ball.x = start.x + (end.x - start.x) * ease; ball.y = start.y + (end.y - start.y) * ease; ball.vx = 0; ball.vy = 0; draw(); if (t < 1) requestAnimationFrame(step); else done(); } requestAnimationFrame(step); }
function addGoal() { const seed = hash(`goal-${state.round}-${state.goals.length}`); const lowest = Math.max(...state.goals.map((goal) => goal.y)); const x = 120 + (seed % 660); const y = clamp(260 + ((seed >>> 4) % 760), 200, lowest + 300); const goal = { id: state.nextGoal, x, y, w: 116, h: 78, done: false, label: String.fromCharCode(64 + state.nextGoal) }; state.goals.push(goal); state.nextGoal += 1; if (state.round >= 2 && state.ownGoals.length === 0) state.ownGoals.push({ id: 'own1', x: 210, y: 1030, w: 128, h: 55 }); return goal; }
function fallLine() { return Math.max(1110, Math.max(...state.goals.map((goal) => goal.y), ...state.ownGoals.map((goal) => goal.y), 0) + 210); }
function loop(ts) { if (!state.last) state.last = ts; const dt = Math.min(0.033, (ts - state.last) / 1000); state.last = ts; update(dt); draw(); requestAnimationFrame(loop); }
function update(dt) { if (state.phase !== 'run') return; state.runTime += dt; for (const gimmick of state.gimmicks) if (gimmick.placed) updateDeviceMotion(gimmick, dt); for (const gimmick of state.gimmicks) if (gimmick.placed) for (const ball of [...state.balls]) applyGimmick(gimmick, ball, dt); for (const ball of [...state.balls]) updateBall(ball, dt); checkCollect(); checkGoals(); const focus = mainBall(); state.cameraY += (focus.y - canvas.height / (canvas.width / WORLD.w) * 0.45 - state.cameraY) * 0.08; const f = fallLine(); for (const ball of [...state.balls]) { if (!ball.main && (ball.y > f || state.runTime >= ball.expiresAt)) state.balls = state.balls.filter((b) => b.id !== ball.id); } if (focus.y > f) { if (state.tutorial === 'intro') { firstFallTutorial(); return; } trialReset('本体が落下。作戦タイム', '分身の落下はセーフ。本体が落ちたので、その試走のゴール通過はリセット。'); } if (state.runTime > 30) trialReset('30秒ゴールなし', 'その試走は終了。ギミックの場所や角度を調整しよう。'); }
function updateBall(ball, dt) { let gv = { x: 0, y: WORLD.gravity }; if (state.runTime < ball.gravityUntil) { const rad = (90 + ball.gravityAngle) * Math.PI / 180; gv = { x: Math.cos(rad) * WORLD.gravity * ball.gravityStrength, y: Math.sin(rad) * WORLD.gravity * ball.gravityStrength }; } ball.vx += gv.x * dt * 60; ball.vy += gv.y * dt * 60; ball.vx *= 0.996; ball.vy *= 0.999; ball.x += ball.vx * dt * 60; ball.y += ball.vy * dt * 60; if (ball.x < ball.r) { ball.x = ball.r; ball.vx = Math.abs(ball.vx) * WORLD.wallBounce; } if (ball.x > WORLD.w - ball.r) { ball.x = WORLD.w - ball.r; ball.vx = -Math.abs(ball.vx) * WORLD.wallBounce; } }
function updateDeviceMotion(g, dt) { const runtime = g.runtime || makeRuntime(); g.runtime = runtime; Object.keys(runtime.cooldowns || {}).forEach((key) => { runtime.cooldowns[key] = Math.max(0, runtime.cooldowns[key] - 1); }); runtime.t += dt; const t = runtime.t; const body = g.body || {}; const p = clamp(body.motionPower ?? 0.55, 0, 1); const hx = g.homeX ?? g.x; const hy = g.homeY ?? g.y; const ha = g.homeAngle ?? g.angle; const prevX = g.x; const prevY = g.y; if (body.motion === 'slide') { g.x = hx + Math.sin(t * 1.25) * (95 + 75 * p); g.y = hy; g.angle = ha; } else if (body.motion === 'bob') { g.x = hx; g.y = hy + Math.sin(t * 1.65) * (55 + 80 * p); g.angle = ha; } else if (body.motion === 'swing' || body.motion === 'pendulum') { g.x = hx; g.y = hy; g.angle = ha + Math.sin(t * 1.28) * (0.45 + 0.75 * p); } else if (body.motion === 'orbit') { const r = 40 + 80 * p; g.x = hx + Math.cos(t * 1.05) * r; g.y = hy + Math.sin(t * 1.05) * r; g.angle = ha + t * 1.05; } else if (body.motion === 'spin') { g.x = hx; g.y = hy; g.angle = ha + t * (1.2 + 2.1 * p); } else { g.x = hx; g.y = hy; g.angle = ha; } runtime.dx = g.x - prevX; runtime.dy = g.y - prevY; }
function applyGimmick(g, ball, dt) { const dx = ball.x - g.x; const dy = ball.y - g.y; const distance = Math.hypot(dx, dy) || 1; if (g.body?.solid && ['line','platform','rail','carrier','arm'].includes(g.body.shape)) collideLine(ball, g.x, g.y, g.angle, bodyLength(g), bodyBounce(g), g.runtime?.dx || 0, g.runtime?.dy || 0); g.motors.forEach((motor, index) => applyMotor(g, ball, motor, index, distance, dx, dy, dt)); applyExit(g, ball, distance, dx, dy, dt); }
function bodyLength(g) { return 110 + clamp(g.body?.size ?? 0.65, 0.35, 1) * 130; }
function bodyBounce(g) { return g.motors.some((m) => ['bumper','flipper','launcher'].includes(m.kind)) ? 1.14 : 0.78; }
function motorRange(motor) { return 42 + clamp(motor.range ?? 0.55, 0.15, 1) * 205; }
function motorPower(motor, base) { return base * clamp(motor.power ?? 0.7, 0.18, 1); }
function motorReady(g, index, ball, frames = 34) { const key = `${index}:${ball.id}`; if ((g.runtime?.cooldowns?.[key] || 0) > 0) return false; g.runtime.cooldowns[key] = frames; return true; }
function nextGoalVector(fromX, fromY) { const next = state.goals.find((goal) => !goal.done) || state.goals[0]; if (!next) return { x: 1, y: -0.15 }; const dx = next.x - fromX; const dy = next.y - fromY; const d = Math.hypot(dx, dy) || 1; return { x: dx / d, y: dy / d }; }
function motorVector(g, motor, dx, dy, ball = mainBall()) { const dir = String(motor.direction || 'angle'); if (dir === 'up') return { x: 0, y: -1 }; if (dir === 'down') return { x: 0, y: 1 }; if (dir === 'left') return { x: -1, y: 0 }; if (dir === 'right') return { x: 1, y: 0 }; if (dir === 'radialIn') { const d = Math.hypot(dx, dy) || 1; return { x: -dx / d, y: -dy / d }; } if (dir === 'radialOut') { const d = Math.hypot(dx, dy) || 1; return { x: dx / d, y: dy / d }; } if (dir === 'tangent') { const d = Math.hypot(dx, dy) || 1; return { x: -dy / d, y: dx / d }; } if (dir === 'towardNextGoal') return nextGoalVector(ball.x, ball.y); if (dir === 'awayFromBall') { const d = Math.hypot(dx, dy) || 1; return { x: -dx / d, y: -dy / d }; } return { x: Math.cos(g.angle), y: Math.sin(g.angle) }; }
function applyMotor(g, ball, motor, index, distance, dx, dy, dt) { const range = motorRange(motor); const near = distance < range; const contact = distance < Math.max(52, 42 + (g.body?.size || 0.65) * 34); const falloff = near ? (1 - distance / range) : 0; const v = motorVector(g, motor, dx, dy, ball); if (motor.kind === 'launcher' && contact && motorReady(g, index, ball, 26)) { launchBall(ball, v, motorPower(motor, 10.8 + (motor.mode === 'cannon' ? 2.4 : 0)), motor.mode === 'spring' ? -0.55 : -0.35); separate(ball, g, 70); } else if (motor.kind === 'bumper' && contact && motorReady(g, index, ball, 18)) { const d = distance || 1; normalizeLaunch(ball, { x: dx / d, y: dy / d - 0.12 }, motorPower(motor, 10.5)); separate(ball, g, 70); } else if (motor.kind === 'fieldForce' && near) addForce(ball, v.x, v.y, motorPower(motor, 0.58) * falloff * dt * 60); else if (motor.kind === 'gravityShift' && near) { ball.gravityAngle = motor.angle; ball.gravityStrength = 0.7 + motor.power * 0.8; ball.gravityUntil = Math.max(ball.gravityUntil, state.runTime + motor.duration); } else if (motor.kind === 'split' && contact && motorReady(g, index, ball, 58)) splitBall(ball, g, motor); else if (motor.kind === 'portal' && contact && motorReady(g, index, ball, 70)) { const out = v; ball.x = clamp(g.x + out.x * (190 + motor.power * 150), 60, WORLD.w - 60); ball.y = Math.max(80, g.y + out.y * (190 + motor.power * 150)); launchBall(ball, out, motorPower(motor, 7.4), -0.6); toast('ワープ！'); } else if (motor.kind === 'rail' && near) railAssist(g, ball, motor, falloff, dt); else if (motor.kind === 'carrier' && contact) { ball.x += (g.runtime?.dx || 0) * 1.04; ball.y += (g.runtime?.dy || 0) * 1.04; ball.vx += (g.runtime?.dx || 0) * 0.18; ball.vy += (g.runtime?.dy || 0) * 0.18; ball.y = Math.min(ball.y, g.y - 34); } else if (motor.kind === 'flipper') collideLine(ball, g.x, g.y, g.angle, bodyLength(g), 1.32, g.runtime?.dx || 0, g.runtime?.dy || 0); else if (motor.kind === 'phase' && contact) { ball.phaseUntil = state.runTime + motor.duration; ball.vy -= motorPower(motor, 1.8); } else if (motor.kind === 'dragZone' && near) { const k = clamp(1 - motorPower(motor, 0.034) * falloff * dt * 60, 0.74, 1); ball.vx *= k; ball.vy *= k; } else if (motor.kind === 'timerRelease') catchRelease(g, ball, motor, index, contact); }
function splitBall(ball, g, motor) { if (state.balls.length >= 10) return; const count = Math.round(clamp(motor.count || 2, 2, 4)); const baseSpeed = Math.max(7.2, Math.hypot(ball.vx, ball.vy), motorPower(motor, 8.8)); const baseAngle = Math.atan2(ball.vy, ball.vx) || g.angle; const spread = (motor.spreadAngle || 38) * Math.PI / 180; const life = clamp(motor.duration || 2.4, 0.8, 4.5); for (let i = 0; i < count - 1 && state.balls.length < 10; i += 1) { const offset = (i - (count - 2) / 2) * spread; const angle = baseAngle + offset; state.balls.push(makeBall({ main: false, generation: ball.generation + 1, x: ball.x + Math.cos(angle) * 16, y: ball.y + Math.sin(angle) * 16, vx: Math.cos(angle) * baseSpeed, vy: Math.sin(angle) * baseSpeed, expiresAt: state.runTime + life })); } const selfAngle = baseAngle - spread * 0.45; ball.vx = Math.cos(selfAngle) * baseSpeed; ball.vy = Math.sin(selfAngle) * baseSpeed; toast('分裂！'); }
function addForce(ball, x, y, f) { ball.vx += x * f; ball.vy += y * f; }
function launchBall(ball, v, speed, extraY = 0) { normalizeLaunch(ball, { x: v.x, y: v.y + extraY }, speed); }
function normalizeLaunch(ball, v, speed) { const d = Math.hypot(v.x, v.y) || 1; ball.vx = v.x / d * speed; ball.vy = v.y / d * speed; }
function applyExit(g, ball, distance, dx, dy, dt) { if (!g.exit) return; const range = Math.max(...g.motors.map(motorRange), 120); if (distance > range) { delete g.runtime.inside[ball.id]; return; } g.runtime.inside[ball.id] = (g.runtime.inside[ball.id] || 0) + dt; const speed = Math.hypot(ball.vx, ball.vy); if (g.runtime.inside[ball.id] > g.exit.afterSeconds || speed < g.exit.minSpeed * 0.55) { const v = motorVector(g, { direction: g.exit.direction || 'towardNextGoal' }, dx, dy, ball); if (speed < g.exit.minSpeed) normalizeLaunch(ball, v, g.exit.minSpeed); else addForce(ball, v.x, v.y, 0.18 * dt * 60); g.runtime.inside[ball.id] = 0; } }
function railAssist(g, ball, motor, falloff, dt) { const ux = Math.cos(g.angle); const uy = Math.sin(g.angle); const vx = ball.x - g.x; const vy = ball.y - g.y; const projection = clamp(vx * ux + vy * uy, -bodyLength(g) / 2, bodyLength(g) / 2); const px = g.x + ux * projection; const py = g.y + uy * projection; ball.x += (px - ball.x) * 0.12; ball.y += (py - ball.y) * 0.12; addForce(ball, ux, uy, motorPower(motor, 0.44) * falloff * dt * 60); }
function catchRelease(g, ball, motor, index, contact) { const key = `${index}:${ball.id}`; if (!g.runtime.holding[key] && contact && motorReady(g, index, ball, 80)) { g.runtime.holding[key] = state.runTime + motor.duration; ball.x = g.x; ball.y = g.y - 36; ball.vx = 0; ball.vy = 0; } if (g.runtime.holding[key]) { ball.x = g.x; ball.y = g.y - 36; ball.vx = 0; ball.vy = 0; if (state.runTime >= g.runtime.holding[key]) { delete g.runtime.holding[key]; launchBall(ball, motorVector(g, motor, ball.x - g.x, ball.y - g.y, ball), motorPower(motor, 10.4), -0.35); } } }
function separate(ball, g, radius) { const dx = ball.x - g.x; const dy = ball.y - g.y; const distance = Math.hypot(dx, dy) || 1; ball.x = g.x + dx / distance * radius; ball.y = g.y + dy / distance * radius; }
function collideLine(ball, cx, cy, angle, length, bounce, moveDx = 0, moveDy = 0) { if (state.runTime < ball.phaseUntil) return; const ux = Math.cos(angle); const uy = Math.sin(angle); const vx = ball.x - cx; const vy = ball.y - cy; const projection = clamp(vx * ux + vy * uy, -length / 2, length / 2); const px = cx + ux * projection; const py = cy + uy * projection; const dx = ball.x - px; const dy = ball.y - py; const distance = Math.hypot(dx, dy) || 1; if (distance < ball.r + 8) { const nx = dx / distance; const ny = dy / distance; const dot = ball.vx * nx + ball.vy * ny; if (dot < 0) { ball.vx -= (1 + bounce) * dot * nx; ball.vy -= (1 + bounce) * dot * ny; ball.vx += moveDx * 0.2; ball.vy += moveDy * 0.2; ball.x = px + nx * (ball.r + 9); ball.y = py + ny * (ball.r + 9); } } }
function checkCollect() { state.fieldEmojis = state.fieldEmojis.filter((emoji) => { const hit = state.balls.some((ball) => Math.hypot(ball.x - emoji.x, ball.y - emoji.y) < ball.r + emoji.r); if (hit) { state.inventory.push({ id: uid(), emoji: emoji.emoji }); toast(`素材 ${emoji.emoji} 回収`); render(); return false; } return true; }); }
function checkGoals() { for (const ownGoal of state.ownGoals) { if (state.balls.some((ball) => inRect(ball, ownGoal))) { ownGoalReset(); return; } } let scored = false; for (const goal of state.goals) { if (!goal.done && state.balls.some((ball) => inRect(ball, goal))) { goal.done = true; scored = true; const focus = mainBall(); focus.vy = Math.min(focus.vy, -5); focus.vx += goal.x < WORLD.w / 2 ? 2 : -2; toast(`GOAL ${goal.label}`); } } if (scored) render(); if (state.goals.every((goal) => goal.done)) roundClearSequence(); }
function inRect(ball, rect) { return Math.abs(ball.x - rect.x) < rect.w / 2 && Math.abs(ball.y - rect.y) < rect.h / 2; }
function render() { els.nav.hidden = !state.tabUnlocked; els.dock.hidden = !state.generateUnlocked || !state.gimmicks.length; els.round.textContent = `R${state.round}`; els.goal.textContent = `${state.goals.filter((goal) => goal.done).length}/${state.goals.length}`; els.emojiCount.textContent = `${state.inventory.length}個`; document.querySelector('[data-tab="generate"]').classList.toggle('is-locked', !state.generateUnlocked); renderEmojis(); renderSelected(); renderDock(); els.kick.disabled = state.phase === 'run' || state.phase === 'between'; }
function renderEmojis() { els.emojiGrid.replaceChildren(); state.inventory.forEach((item) => { const button = document.createElement('button'); button.className = 'emoji-btn'; button.textContent = item.emoji; button.type = 'button'; if (state.selected.some((selected) => selected.id === item.id)) button.classList.add('is-selected'); button.onclick = () => toggleEmoji(item); els.emojiGrid.appendChild(button); }); }
function toggleEmoji(item) { const index = state.selected.findIndex((selected) => selected.id === item.id); if (index >= 0) state.selected.splice(index, 1); else { if (state.selected.length >= 3) state.selected.shift(); state.selected.push(item); } render(); }
function renderSelected() { els.selectedRow.replaceChildren(); for (let i = 0; i < 3; i += 1) { const slot = document.createElement('button'); slot.type = 'button'; slot.className = 'slot'; if (state.selected[i]) { slot.textContent = state.selected[i].emoji; slot.onclick = () => { state.selected.splice(i, 1); render(); }; } else { slot.textContent = '空'; slot.classList.add('empty'); } els.selectedRow.appendChild(slot); } els.generate.disabled = state.selected.length !== 3; }
function renderDock() { els.dockList.replaceChildren(); state.gimmicks.forEach((g) => { const card = document.createElement('article'); card.className = 'gimmick-card'; if (state.focusGimmick === g.id || state.placing === g.id) card.classList.add('is-selected'); const motorText = g.motors.map((m) => MOTOR_META[m.kind]?.label || m.kind).join('＋'); const motionText = g.body?.motion && g.body.motion !== 'none' ? ` / ${motionLabel(g.body.motion)}` : ''; card.innerHTML = `<span class="gimmick-name">${g.recipe.join('')} ${escapeHtml(g.name)}</span><div class="gimmick-meta">${escapeHtml(g.visualLabel || '装置')} / ${escapeHtml(motorText)}${escapeHtml(motionText)}</div><div class="gimmick-help">${escapeHtml(g.shortEffect || 'ボールの動きを変える')}</div>`; card.onpointerdown = (event) => startCardDrag(event, g); card.onclick = () => { state.placing = g.id; state.focusGimmick = g.id; setCoach('ギミックを選択中', `${g.visualLabel || '装置'}：${g.shortEffect || 'ボールの動きを変える'}`); showHelp('コートをタップ、またはカードをドラッグすると設置できます。'); render(); draw(); }; els.dockList.appendChild(card); }); }
function motionLabel(motion) { return { spin: '回転', slide: '左右移動', bob: '上下移動', swing: '揺れ', orbit: '周回', pendulum: '振り子' }[motion] || motion; }
function escapeHtml(text) { return String(text).replace(/[&<>"']/g, (char) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#039;' }[char])); }
function fit() { const rect = canvas.getBoundingClientRect(); const dpr = Math.min(2, window.devicePixelRatio || 1); const w = Math.floor(rect.width * dpr); const h = Math.floor(rect.height * dpr); if (w > 0 && h > 0 && (canvas.width !== w || canvas.height !== h)) { canvas.width = w; canvas.height = h; } }
function fitCameraToGoals() { fit(); const scale = canvas.width / WORLD.w; const viewH = canvas.height / scale; const ys = [START.y, ...state.goals.map((goal) => goal.y)]; const minY = Math.min(...ys) - 120; const maxY = Math.max(...ys) + 180; state.cameraY = clamp((minY + maxY - viewH) / 2, -120, fallLine() - viewH + 120); draw(); }
function focusCameraOn(y) { fit(); const scale = canvas.width / WORLD.w; const viewH = canvas.height / scale; state.cameraY = clamp(y - viewH * 0.45, -120, fallLine() - viewH + 120); draw(); }
function draw() { if (!state) return; fit(); const scale = canvas.width / WORLD.w; const viewH = canvas.height / scale; state.cameraY = clamp(state.cameraY, -120, fallLine() - viewH + 120); ctx.clearRect(0, 0, canvas.width, canvas.height); ctx.save(); ctx.scale(scale, scale); ctx.translate(0, -state.cameraY); drawField(viewH); state.fieldEmojis.forEach(drawEmoji); state.goals.forEach(drawGoal); state.ownGoals.forEach(drawOwn); state.gimmicks.filter((g) => g.placed).forEach(drawGimmick); state.balls.forEach(drawBall); ctx.restore(); }
function drawField(viewH) { const top = state.cameraY - 80; const bottom = state.cameraY + viewH + 80; ctx.fillStyle = '#0f713e'; ctx.fillRect(0, top, WORLD.w, bottom - top); ctx.strokeStyle = 'rgba(255,255,255,.14)'; ctx.lineWidth = 2; for (let y = Math.floor(top / 80) * 80; y < bottom; y += 80) { ctx.beginPath(); ctx.moveTo(0, y); ctx.lineTo(WORLD.w, y); ctx.stroke(); } ctx.strokeStyle = 'rgba(232,255,89,.28)'; ctx.lineWidth = 5; round(18, top, WORLD.w - 36, bottom - top, 28); ctx.stroke(); const f = fallLine(); ctx.fillStyle = 'rgba(255,82,104,.16)'; ctx.fillRect(0, f, WORLD.w, 90); ctx.strokeStyle = '#ff5268'; ctx.setLineDash([18, 12]); ctx.beginPath(); ctx.moveTo(0, f); ctx.lineTo(WORLD.w, f); ctx.stroke(); ctx.setLineDash([]); }
function drawEmoji(emoji) { ctx.save(); ctx.translate(emoji.x, emoji.y); ctx.shadowColor = 'rgba(232,255,89,.7)'; ctx.shadowBlur = 14; ctx.font = '31px system-ui'; ctx.textAlign = 'center'; ctx.textBaseline = 'middle'; ctx.fillText(emoji.emoji, 0, 0); ctx.restore(); }
function drawGoal(goal) { ctx.save(); ctx.translate(goal.x, goal.y); ctx.globalAlpha = goal.done ? 0.45 : 1; ctx.strokeStyle = goal.done ? '#d5ddd5' : '#e8ff59'; ctx.fillStyle = goal.done ? 'rgba(220,220,220,.08)' : 'rgba(232,255,89,.13)'; ctx.lineWidth = 9; round(-goal.w / 2, -goal.h / 2, goal.w, goal.h, 18); ctx.fill(); ctx.stroke(); ctx.strokeStyle = 'rgba(255,255,255,.28)'; ctx.lineWidth = 2; for (let x = -goal.w / 2 + 18; x < goal.w / 2; x += 18) { ctx.beginPath(); ctx.moveTo(x, -goal.h / 2); ctx.lineTo(x, goal.h / 2); ctx.stroke(); } ctx.fillStyle = '#fff'; ctx.font = '900 18px system-ui'; ctx.textAlign = 'center'; ctx.fillText(goal.done ? 'CLEAR' : `GOAL ${goal.label}`, 0, -goal.h / 2 - 12); ctx.restore(); }
function drawOwn(ownGoal) { ctx.save(); ctx.translate(ownGoal.x, ownGoal.y); ctx.fillStyle = 'rgba(255,82,104,.22)'; ctx.strokeStyle = '#ff5268'; ctx.lineWidth = 7; round(-ownGoal.w / 2, -ownGoal.h / 2, ownGoal.w, ownGoal.h, 14); ctx.fill(); ctx.stroke(); ctx.fillStyle = '#fff'; ctx.font = '900 24px system-ui'; ctx.textAlign = 'center'; ctx.fillText('☠️', 0, 8); ctx.restore(); }
function drawGimmick(g) { const primary = g.motors[0] || { kind: 'launcher', range: 0.5 }; const meta = MOTOR_META[primary.kind] || MOTOR_META.launcher; const shape = g.body?.shape || defaultShapeForMotors(g.motors); const focused = state.focusGimmick === g.id || state.placing === g.id; drawMotionPath(g, meta, focused); ctx.save(); ctx.translate(g.x, g.y); ctx.rotate(g.angle); ctx.strokeStyle = meta.color; ctx.fillStyle = `${meta.color}33`; ctx.lineWidth = 6; const length = bodyLength(g); if (shape === 'fan') { ctx.beginPath(); ctx.moveTo(0, 0); ctx.arc(0, 0, 62, -0.62, 0.62); ctx.closePath(); ctx.fill(); ctx.stroke(); arrow(70, meta.color); } else if (shape === 'area') { ctx.beginPath(); ctx.arc(0, 0, 46, 0, Math.PI * 2); ctx.fill(); ctx.stroke(); } else if (shape === 'gate') { ctx.beginPath(); ctx.ellipse(0, 0, 38, 52, 0, 0, Math.PI * 2); ctx.fill(); ctx.stroke(); } else if (['line','platform','rail','carrier','arm'].includes(shape)) { ctx.beginPath(); ctx.moveTo(-length / 2, 0); ctx.lineTo(length / 2, 0); ctx.stroke(); if (shape === 'carrier') { round(-length / 2, -18, length, 36, 18); ctx.fill(); ctx.stroke(); } } else { ctx.beginPath(); ctx.arc(0, 0, 42, 0, Math.PI * 2); ctx.fill(); ctx.stroke(); arrow(58, meta.color); } ctx.rotate(-g.angle); ctx.fillStyle = '#fff'; ctx.font = '23px system-ui'; ctx.textAlign = 'center'; ctx.fillText(meta.icon, 0, -24); ctx.font = '900 15px system-ui'; ctx.fillText(g.visualLabel || meta.label, 0, 31); if (focused) drawHandle(g); ctx.restore(); drawRangeHints(g, meta, focused); }
function drawMotionPath(g, meta, focused) { if (!focused && state.phase === 'run') return; const motion = g.body?.motion || 'none'; if (motion === 'none') return; ctx.save(); ctx.strokeStyle = meta.color; ctx.globalAlpha = 0.28; ctx.lineWidth = 4; ctx.setLineDash([14, 10]); const hx = g.homeX ?? g.x; const hy = g.homeY ?? g.y; const p = clamp(g.body?.motionPower ?? 0.55, 0, 1); if (motion === 'slide') { const amp = 95 + 75 * p; ctx.beginPath(); ctx.moveTo(hx - amp, hy); ctx.lineTo(hx + amp, hy); ctx.stroke(); } else if (motion === 'bob') { const amp = 55 + 80 * p; ctx.beginPath(); ctx.moveTo(hx, hy - amp); ctx.lineTo(hx, hy + amp); ctx.stroke(); } else if (motion === 'orbit') { const r = 40 + 80 * p; ctx.beginPath(); ctx.arc(hx, hy, r, 0, Math.PI * 2); ctx.stroke(); } else if (motion === 'swing' || motion === 'pendulum') { ctx.beginPath(); ctx.arc(hx, hy, 86, -1.1, 1.1); ctx.stroke(); } else if (motion === 'spin') { ctx.beginPath(); ctx.arc(hx, hy, 72, 0, Math.PI * 2); ctx.stroke(); } ctx.setLineDash([]); ctx.restore(); }
function drawRangeHints(g, meta, focused) { if (!focused && state.phase === 'run') return; const fieldMotors = g.motors.filter((m) => ['fieldForce','gravityShift','dragZone','split'].includes(m.kind)); if (!fieldMotors.length && !g.exit) return; const maxRange = Math.max(...fieldMotors.map(motorRange), g.exit ? 145 : 0); ctx.save(); ctx.translate(g.x, g.y); ctx.strokeStyle = meta.color; ctx.globalAlpha = 0.24; ctx.lineWidth = 4; ctx.beginPath(); ctx.arc(0, 0, maxRange, 0, Math.PI * 2); ctx.stroke(); ctx.globalAlpha = 0.82; ctx.fillStyle = meta.color; ctx.font = '900 15px system-ui'; ctx.textAlign = 'center'; ctx.fillText(g.exit ? '出口つき範囲' : `${MOTOR_META[fieldMotors[0].kind]?.label || '効果'}範囲`, 0, -maxRange - 10); ctx.restore(); }
function drawHandle(g) { const hx = Math.cos(g.angle) * 104; const hy = Math.sin(g.angle) * 104; ctx.save(); ctx.strokeStyle = '#e8ff59'; ctx.lineWidth = 4; ctx.beginPath(); ctx.moveTo(0, 0); ctx.lineTo(hx, hy); ctx.stroke(); ctx.fillStyle = '#e8ff59'; ctx.beginPath(); ctx.arc(hx, hy, 18, 0, Math.PI * 2); ctx.fill(); ctx.fillStyle = '#06150e'; ctx.font = '900 13px system-ui'; ctx.textAlign = 'center'; ctx.textBaseline = 'middle'; ctx.fillText('回転', hx, hy); ctx.restore(); }
function arrow(length, color) { ctx.strokeStyle = color; ctx.fillStyle = color; ctx.lineWidth = 5; ctx.beginPath(); ctx.moveTo(-length / 2, 0); ctx.lineTo(length / 2, 0); ctx.stroke(); ctx.beginPath(); ctx.moveTo(length / 2 + 7, 0); ctx.lineTo(length / 2 - 10, -9); ctx.lineTo(length / 2 - 10, 9); ctx.closePath(); ctx.fill(); }
function drawBall(ball) { ctx.save(); ctx.translate(ball.x, ball.y); ctx.globalAlpha = ball.main ? 1 : clamp((ball.expiresAt - state.runTime) / 1.2, 0.25, 0.85); ctx.fillStyle = ball.main ? '#fff' : '#d7ccff'; ctx.strokeStyle = ball.main ? '#111' : '#6b4dff'; ctx.lineWidth = ball.main ? 4 : 3; ctx.beginPath(); ctx.arc(0, 0, ball.r, 0, Math.PI * 2); ctx.fill(); ctx.stroke(); ctx.font = `${ball.main ? 19 : 15}px system-ui`; ctx.textAlign = 'center'; ctx.textBaseline = 'middle'; ctx.fillText(ball.main ? '⚽' : '⚽︎', 0, 1); ctx.restore(); }
function round(x, y, w, h, r) { ctx.beginPath(); ctx.moveTo(x + r, y); ctx.lineTo(x + w - r, y); ctx.quadraticCurveTo(x + w, y, x + w, y + r); ctx.lineTo(x + w, y + h - r); ctx.quadraticCurveTo(x + w, y + h, x + w - r, y + h); ctx.lineTo(x + r, y + h); ctx.quadraticCurveTo(x, y + h, x, y + h - r); ctx.lineTo(x, y + r); ctx.quadraticCurveTo(x, y, x + r, y); }
function worldFromEvent(event) { const rect = canvas.getBoundingClientRect(); const scale = canvas.width / WORLD.w; return { x: (event.clientX - rect.left) * (canvas.width / rect.width) / scale, y: (event.clientY - rect.top) * (canvas.height / rect.height) / scale + state.cameraY, inside: event.clientX >= rect.left && event.clientX <= rect.right && event.clientY >= rect.top && event.clientY <= rect.bottom }; }
function hitGimmick(point) { return [...state.gimmicks].reverse().find((g) => g.placed && Math.hypot(g.x - point.x, g.y - point.y) < 85); }
function handlePos(g) { return { x: g.x + Math.cos(g.angle) * 104, y: g.y + Math.sin(g.angle) * 104 }; }
function startCardDrag(event, g) { event.preventDefault(); state.placing = g.id; state.focusGimmick = g.id; drag = { type: 'new', id: g.id }; showHelp(`${g.visualLabel || '装置'}をコートへドラッグすると設置できます。`); render(); }
function onDown(event) { if (state.phase === 'run' || state.phase === 'between' || state.gameover) return; const point = worldFromEvent(event); const handleTarget = [...state.gimmicks].reverse().find((g) => g.placed && Math.hypot(handlePos(g).x - point.x, handlePos(g).y - point.y) < 34); if (handleTarget) { state.focusGimmick = handleTarget.id; drag = { type: 'rotate', id: handleTarget.id }; showHelp('黄色い「回転」ハンドルで角度を変えられます。'); return; } const hit = hitGimmick(point); if (hit) { state.focusGimmick = hit.id; drag = { type: 'move', id: hit.id, dx: hit.x - point.x, dy: hit.y - point.y }; showHelp('ドラッグで移動。黄色い「回転」ハンドルで角度変更。'); render(); draw(); return; } if (state.placing) { const g = state.gimmicks.find((item) => item.id === state.placing); if (g) { g.x = point.x; g.y = point.y; g.placed = true; saveGimmickHome(g); state.focusGimmick = g.id; drag = { type: 'move', id: g.id, dx: 0, dy: 0 }; render(); draw(); return; } } drag = { type: 'pan', startY: event.clientY, camera: state.cameraY }; }
function onMove(event) { if (!drag) return; event.preventDefault(); const point = worldFromEvent(event); const g = state.gimmicks.find((item) => item.id === drag.id); if (drag.type === 'new' && point.inside && g) { g.x = point.x; g.y = point.y; g.placed = true; state.focusGimmick = g.id; saveGimmickHome(g); draw(); return; } if (drag.type === 'move' && g) { g.x = clamp(point.x + (drag.dx || 0), 40, WORLD.w - 40); g.y = point.y + (drag.dy || 0); saveGimmickHome(g); draw(); return; } if (drag.type === 'rotate' && g) { g.angle = Math.atan2(point.y - g.y, point.x - g.x); saveGimmickHome(g); draw(); return; } if (drag.type === 'pan') { state.cameraY = drag.camera - (event.clientY - drag.startY) / (canvas.width / WORLD.w); draw(); } }
function onUp() { if (!drag) return; const g = state.gimmicks.find((item) => item.id === drag.id); if (g && g.placed) { saveGimmickHome(g); state.placing = null; state.focusGimmick = g.id; hideHelp(); setCoach('配置完了', `${g.visualLabel || '装置'}：${g.shortEffect || 'ボールの動きを変える'}`); if (state.tutorial === 'place') { showModal('角度も変えられる', '編集もサッカーのうち', '置いたギミックに出ている黄色い「回転」ハンドルをドラッグすると角度が変わります。調整したらキックオフ！', '了解', () => closeModal()); state.tutorial = 'edit'; } } drag = null; render(); draw(); }
els.modalPrimary.onclick = () => { if (modalAction) modalAction(); };
els.modalSecondary.onclick = closeModal;
els.kick.onclick = startRun;
els.center.onclick = fitCameraToGoals;
els.generate.onclick = generateGimmick;
document.querySelectorAll('.tab-btn').forEach((button) => { button.onclick = () => setTab(button.dataset.tab); });
canvas.addEventListener('pointerdown', onDown, { passive: false });
window.addEventListener('pointermove', onMove, { passive: false });
window.addEventListener('pointerup', onUp);
window.addEventListener('resize', fitCameraToGoals);
boot();
