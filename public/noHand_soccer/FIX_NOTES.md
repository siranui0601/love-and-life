# Follow-up UX fixes

This file exists to make the follow-up branch easy to identify in GitHub if the prior UX PR was already merged during review.

The actual fixes are in `index.html`, `style.css`, and `app.js`.
