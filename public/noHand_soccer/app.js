const canvas = document.querySelector('#gameCanvas');
const ctx = canvas.getContext('2d');
const $ = (s) => document.querySelector(s);

const els = {
  venue: $('#venueScreen'), gen: $('#generateScreen'), nav: $('#bottomNav'),
  kick: $('#kickBtn'), center: $('#centerBtn'), generate: $('#generateBtn'),
  dock: $('#gimmickDock'), dockList: $('#dockList'), fieldHelp: $('#fieldHelp'),
  coachTitle: $('#coachTitle'), coachText: $('#coachText'),
  round: $('#roundLabel'), goal: $('#goalLabel'), emojiCount: $('#emojiCountLabel'), emojiGrid: $('#emojiGrid'),
  selectedRow: $('#selectedRow'), modal: $('#modal'), modalKicker: $('#modalKicker'), modalTitle: $('#modalTitle'),
  modalText: $('#modalText'), modalPrimary: $('#modalPrimary'), modalSecondary: $('#modalSecondary'), toast: $('#toast'),
};

const WORLD = { w: 900, gravity: 0.36, wallBounce: 0.78 };
const START = { x: 450, y: 165 };
const FALLBACK_EMOJIS = ['👻','🦀','🚀','💪🏾','🧱','🥝','🧲','🏘️','🐸','🎈','🍌','🌪️','🚪','🛞','🧊','☁️','⚡','🕳️','🪜','🧵','⚙️','🛝','🦘','🪭','🪨','🔘'];
let emojiPool = [...FALLBACK_EMOJIS];
const TEMPLATES = ['BOUNCE','LAUNCH','FLOW','SHAPE','WARP','ATTACH','ROTATE','CARRY','PHASE','SWITCH'];
const META = {
  BOUNCE:{label:'反発',color:'#e8ff59',icon:'🟩'}, LAUNCH:{label:'発射',color:'#ffbd59',icon:'🚀'}, FLOW:{label:'風・流れ',color:'#76e4ff',icon:'🌪️'},
  SHAPE:{label:'地形',color:'#fff',icon:'🛝'}, WARP:{label:'ワープ',color:'#b28cff',icon:'🚪'}, ATTACH:{label:'糸・連結',color:'#ff8ad1',icon:'🧵'},
  ROTATE:{label:'回転',color:'#9cffc1',icon:'⚙️'}, CARRY:{label:'運搬',color:'#ffdf70',icon:'☁️'}, PHASE:{label:'透過',color:'#c8d8ff',icon:'👻'}, SWITCH:{label:'切替',color:'#ff9b9b',icon:'🔘'}
};

let state = freshState();
let drag = null;
let modalAction = null;
let toastTimer = null;

function freshState(){return {tab:'venue',tutorial:'intro',phase:'ready',round:1,cameraY:0,targetCameraY:0,runTime:0,ball:{x:START.x,y:START.y,vx:0,vy:0,r:18,phaseUntil:0},goals:[{id:1,x:690,y:360,w:116,h:78,done:false,label:'A'}],ownGoals:[],fieldEmojis:[],inventory:[],selected:[],gimmicks:[],placing:null,focusGimmick:null,nextGimmick:1,nextGoal:2,generateUnlocked:false,tabUnlocked:false,gameover:false,last:0};}
function uid(){return Math.random().toString(36).slice(2)+Date.now().toString(36)}
function hash(str){let h=2166136261;for(let i=0;i<str.length;i++){h^=str.charCodeAt(i);h=Math.imul(h,16777619)}return h>>>0}
function pick(arr,n){return arr[Math.abs(Number(n)||0)%arr.length]}
function clamp(v,min,max){return Math.max(min,Math.min(max,v))}
function toast(msg){els.toast.textContent=msg;els.toast.hidden=false;clearTimeout(toastTimer);toastTimer=setTimeout(()=>els.toast.hidden=true,1800)}
function setCoach(title,text){els.coachTitle.textContent=title;els.coachText.textContent=text}
function showHelp(text){els.fieldHelp.textContent=text;els.fieldHelp.hidden=false}
function hideHelp(){els.fieldHelp.hidden=true}
function showModal(kicker,title,text,primary,action,secondary){els.modalKicker.textContent=kicker;els.modalTitle.textContent=title;els.modalText.textContent=text;els.modalPrimary.textContent=primary;els.modalSecondary.hidden=!secondary;els.modalSecondary.textContent=secondary||'';modalAction=action;els.modal.classList.add('is-open')}
function closeModal(){els.modal.classList.remove('is-open')}
async function loadEmojiCatalog(){try{const res=await fetch('./emoji_catalog_full_ja.json',{cache:'force-cache'});if(!res.ok)return;const data=await res.json();const list=Array.isArray(data)?data:Object.values(data).flat();const normalized=list.map(item=>typeof item==='string'?item:(item.emoji||item.char||item.symbol||item.unicode||'')).filter(Boolean);if(normalized.length>30)emojiPool=normalized}catch(_){} }
async function boot(){await loadEmojiCatalog();grantEmojis(8);render();fitCameraToGoals();showModal('KICK OFF','今宵はサッカー！','ボールを黄色いゴールへ入れるゲームです。まずは会場を見て、下のキックオフを押そう⚽️','会場を見る',()=>{closeModal();showHelp('下の「キックオフ」を押すとボールが動きます。黄色いGOAL Aへ入れよう。')});requestAnimationFrame(loop)}
function grantEmojis(n){for(let i=0;i<n;i++){state.inventory.push({id:uid(),emoji:pick(emojiPool,hash(`${Date.now()}-${i}-${state.inventory.length}`))})}}
function spawnFieldEmojis(n){const base=hash(`field-${state.round}-${state.fieldEmojis.length}`);for(let i=0;i<n;i++){state.fieldEmojis.push({id:uid(),emoji:pick(emojiPool,base+i*37),x:110+((base+i*91)%680),y:250+((base+i*141)%640),r:20})}}
function setTab(tab){if(tab==='generate'&&!state.generateUnlocked){toast('まずキックオフして、細工を解禁しよう');return}state.tab=tab;els.venue.classList.toggle('is-active',tab==='venue');els.gen.classList.toggle('is-active',tab==='generate');document.querySelectorAll('.tab-btn').forEach(b=>b.classList.toggle('is-active',b.dataset.tab===tab));render();if(tab==='venue')fitCameraToGoals()}
function startRun(){if(state.phase==='run'||state.phase==='between')return;if(state.gameover){resetAll();return}state.phase='run';state.runTime=0;state.ball={x:START.x,y:START.y,vx:0,vy:0,r:18,phaseUntil:0};setCoach('キックオフ中','ボールを見守ろう。素材絵文字はボールで触れると回収できます。');hideHelp();render();fitCameraToGoals()}
function enterEdit(title,text){state.phase='edit';state.ball={x:START.x,y:START.y,vx:0,vy:0,r:18,phaseUntil:0};setCoach(title||'作戦タイム',text||'キックオフ中でなければ、ギミックを自由に配置・移動できます。');render();fitCameraToGoals()}
function resetAll(){state=freshState();grantEmojis(8);render();fitCameraToGoals();showModal('KICK OFF','今宵はサッカー！','ボールを黄色いゴールへ入れるゲームです。まずは会場を見て、下のキックオフを押そう⚽️','会場を見る',()=>{closeModal();showHelp('下の「キックオフ」を押すとボールが動きます。黄色いGOAL Aへ入れよう。')})}
function firstFallTutorial(){state.generateUnlocked=true;state.tabUnlocked=true;els.nav.hidden=false;enterEdit('落下！このままだと届かない','下の「生成」タブで絵文字を3つ選び、ボールを助けるギミックを作ろう。');showModal('作戦タイム','そうだ、細工をしよう！','このままだとボールは落ちます。生成タブで絵文字を3つ選んで、ゴールへ導くギミックを作ろう。','生成タブへ',()=>{closeModal();setTab('generate')});state.tutorial='generate'}
function failGame(reason){state.gameover=true;state.phase='gameover';render();draw();showModal('GAME OVER',reason,'赤いゴールは即終了。配置を変えれば、まだサッカーと言い張れます。','最初から',()=>{closeModal();resetAll()},'閉じる')}
function generateGimmick(){if(state.selected.length!==3){toast('絵文字を3つ選んで！');return}const recipe=state.selected.map(e=>e.emoji);const seed=hash(recipe.join('|'));const template=pick(TEMPLATES,seed);const adj=pick(['合法','会場騒然','反則気味','ふわふわ','逆転','VAR泣かせ','不器用','謎の'],seed>>>3);const name=`${adj}${recipe.join('')}`;const g={id:'g'+state.nextGimmick++,recipe,template,name,x:450,y:520,angle:(seed%360)*Math.PI/180,power:1+(seed%5)*.13,placed:false,spin:0};state.gimmicks.push(g);for(const item of state.selected){const idx=state.inventory.findIndex(e=>e.id===item.id);if(idx>=0)state.inventory.splice(idx,1)}state.selected=[];state.placing=g.id;state.focusGimmick=g.id;setTab('venue');setCoach('ギミックを設置しよう','下のギミックカードをコートへドラッグすると設置できます。');showHelp(`「${name}」を設置できます。下のカードを押したままコートへドラッグ。`);if(state.tutorial==='generate'){showModal('配置しよう','会場へ戻った！','下のギミックカードを押したまま、コートへドラッグして離そう。設置後は本体ドラッグで移動できます。','配置する',()=>closeModal());state.tutorial='place'}render();fitCameraToGoals()}
function roundClearSequence(){state.phase='between';render();showModal('GOOOOL!','GOOOOL！','ゴール成功！ 次はコートが広がり、新しいゴールが増えます。','次へ',()=>{closeModal();animateBallHome(()=>{state.round++;state.goals.forEach(g=>g.done=false);const newGoal=addGoal();render();focusCameraOn(newGoal.y);setTimeout(()=>{fitCameraToGoals();setTimeout(()=>showAfterGoalTutorial(),450)},650)})})}
function showAfterGoalTutorial(){showModal('ROUND UP','ゴールが増えた！','大丈夫、ゴールが増えただけだ！素材が足りない……？ 安心してくれ。','素材を見る',()=>{closeModal();spawnFieldEmojis(5);render();showModal('素材出現','絵文字が出現した！','ゴールを決めると、コートに絵文字が出ます。ボールで触れると素材回収。素材3つでギミックを作り、全ゴールを完遂するんだ！','了解',()=>{closeModal();enterEdit('全ゴールをくぐろう','黄色いゴールをすべて通過すると次ラウンド。絵文字はボールで触れると素材になります。')})})}
function animateBallHome(done){const start={x:state.ball.x,y:state.ball.y};const end={...START};const t0=performance.now();function step(now){const t=clamp((now-t0)/650,0,1);const e=1-Math.pow(1-t,3);state.ball.x=start.x+(end.x-start.x)*e;state.ball.y=start.y+(end.y-start.y)*e;state.ball.vx=0;state.ball.vy=0;draw();if(t<1)requestAnimationFrame(step);else done()}requestAnimationFrame(step)}
function addGoal(){const seed=hash(`goal-${state.round}-${state.goals.length}`);const lowest=Math.max(...state.goals.map(g=>g.y));const x=120+(seed%660);const y=clamp(260+((seed>>>4)%760),200,lowest+300);const goal={id:state.nextGoal,x,y,w:116,h:78,done:false,label:String.fromCharCode(64+state.nextGoal)};state.goals.push(goal);state.nextGoal++;if(state.round>=2&&state.ownGoals.length===0)state.ownGoals.push({id:'own1',x:210,y:1030,w:128,h:55});return goal}
function fallLine(){return Math.max(1110,Math.max(...state.goals.map(g=>g.y),...state.ownGoals.map(g=>g.y),0)+210)}
function loop(ts){if(!state.last)state.last=ts;const dt=Math.min(.033,(ts-state.last)/1000);state.last=ts;update(dt);draw();requestAnimationFrame(loop)}
function update(dt){if(state.phase!=='run')return;const b=state.ball;state.runTime+=dt;for(const g of state.gimmicks){if(g.placed)applyGimmick(g,dt)}b.vy+=WORLD.gravity*dt*60;b.vx*=.996;b.vy*=.999;b.x+=b.vx*dt*60;b.y+=b.vy*dt*60;if(b.x<b.r){b.x=b.r;b.vx=Math.abs(b.vx)*WORLD.wallBounce}if(b.x>WORLD.w-b.r){b.x=WORLD.w-b.r;b.vx=-Math.abs(b.vx)*WORLD.wallBounce}checkCollect();checkGoals();state.cameraY+=(b.y-canvas.height/(canvas.width/WORLD.w)*.45-state.cameraY)*.08;if(b.y>fallLine()){if(state.tutorial==='intro'){firstFallTutorial();return}enterEdit('落下。作戦タイム','キックオフ中ではないので、ギミックを配置・移動できます。')}if(state.runTime>30)enterEdit('30秒ゴールなし','ギミックの場所や角度を調整しよう。')}
function applyGimmick(g,dt){const b=state.ball,dx=b.x-g.x,dy=b.y-g.y,d=Math.hypot(dx,dy)||1;if(g.template==='FLOW'&&d<145){const f=(1-d/145)*.24*g.power;b.vx+=Math.cos(g.angle)*f*dt*60;b.vy+=Math.sin(g.angle)*f*dt*60}if(g.template==='ATTACH'&&d<190){b.vx+=(g.x-b.x)*.0009*dt*60;b.vy+=(g.y-b.y)*.0009*dt*60}if(g.template==='CARRY'&&Math.abs(dx)<66&&Math.abs(dy)<34&&b.vy>-3){b.y=g.y-36;b.vx+=Math.cos(g.angle)*.09;b.vy*=.8}if(g.template==='ROTATE')g.spin+=dt*2.2;collideGimmick(g,d,dx,dy)}
function collideGimmick(g,d,dx,dy){const b=state.ball;if(g.template==='BOUNCE'&&d<58){const nx=dx/d,ny=dy/d;b.vx=nx*5.7*g.power+Math.cos(g.angle);b.vy=ny*5.7*g.power+Math.sin(g.angle);separate(g,62)}if(g.template==='LAUNCH'&&d<58){b.vx=Math.cos(g.angle)*7.2*g.power;b.vy=Math.sin(g.angle)*7.2*g.power;separate(g,62)}if(g.template==='SHAPE')collideLine(g.x,g.y,g.angle,155,.76);if(g.template==='ROTATE')collideLine(g.x,g.y,g.angle+g.spin,135,1.08);if(g.template==='WARP'&&d<42&&!g.cool){b.x=WORLD.w-g.x;b.y=Math.max(80,g.y-260);b.vy=Math.min(b.vy,-4);g.cool=60;toast('ワープ！')}if(g.cool)g.cool--;if(g.template==='PHASE'&&d<52){b.phaseUntil=state.runTime+2.5;b.vy-=1.1}if(g.template==='SWITCH'&&d<55&&!g.cool){state.gimmicks.forEach(o=>{if(o.id!==g.id&&Math.hypot(o.x-g.x,o.y-g.y)<230)o.angle+=Math.PI/5});g.cool=70;toast('近くの向きが変わった')}}
function separate(g,r){const b=state.ball,dx=b.x-g.x,dy=b.y-g.y,d=Math.hypot(dx,dy)||1;b.x=g.x+dx/d*r;b.y=g.y+dy/d*r}
function collideLine(cx,cy,ang,len,bounce){const b=state.ball,ux=Math.cos(ang),uy=Math.sin(ang),vx=b.x-cx,vy=b.y-cy,p=clamp(vx*ux+vy*uy,-len/2,len/2);const px=cx+ux*p,py=cy+uy*p,dx=b.x-px,dy=b.y-py,d=Math.hypot(dx,dy)||1;if(d<b.r+8){const nx=dx/d,ny=dy/d,dot=b.vx*nx+b.vy*ny;if(dot<0){b.vx-=(1+bounce)*dot*nx;b.vy-=(1+bounce)*dot*ny;b.x=px+nx*(b.r+9);b.y=py+ny*(b.r+9)}}}
function checkCollect(){state.fieldEmojis=state.fieldEmojis.filter(e=>{if(Math.hypot(state.ball.x-e.x,state.ball.y-e.y)<state.ball.r+e.r){state.inventory.push({id:uid(),emoji:e.emoji});toast(`素材 ${e.emoji} 回収`);render();return false}return true})}
function checkGoals(){for(const o of state.ownGoals)if(inRect(state.ball,o))return failGame('オウンゴール！');let scored=false;for(const g of state.goals)if(!g.done&&inRect(state.ball,g)){g.done=true;scored=true;state.ball.vy=Math.min(state.ball.vy,-5);state.ball.vx+=(g.x<WORLD.w/2?2:-2);toast(`GOAL ${g.label}`)}if(scored)render();if(state.goals.every(g=>g.done))roundClearSequence()}
function inRect(b,r){return Math.abs(b.x-r.x)<r.w/2&&Math.abs(b.y-r.y)<r.h/2}
function render(){els.nav.hidden=!state.tabUnlocked;els.dock.hidden=!state.generateUnlocked||!state.gimmicks.length;els.round.textContent=`R${state.round}`;els.goal.textContent=`${state.goals.filter(g=>g.done).length}/${state.goals.length}`;els.emojiCount.textContent=`${state.inventory.length}個`;document.querySelector('[data-tab="generate"]').classList.toggle('is-locked',!state.generateUnlocked);renderEmojis();renderSelected();renderDock();els.kick.disabled=state.phase==='run'||state.phase==='between'}
function renderEmojis(){els.emojiGrid.replaceChildren();state.inventory.forEach(item=>{const b=document.createElement('button');b.className='emoji-btn';b.textContent=item.emoji;b.type='button';if(state.selected.some(s=>s.id===item.id))b.classList.add('is-selected');b.onclick=()=>toggleEmoji(item);els.emojiGrid.appendChild(b)})}
function toggleEmoji(item){const i=state.selected.findIndex(s=>s.id===item.id);if(i>=0)state.selected.splice(i,1);else{if(state.selected.length>=3)state.selected.shift();state.selected.push(item)}render()}
function renderSelected(){els.selectedRow.replaceChildren();for(let i=0;i<3;i++){const d=document.createElement('button');d.type='button';d.className='slot';if(state.selected[i]){d.textContent=state.selected[i].emoji;d.onclick=()=>{state.selected.splice(i,1);render()}}else{d.textContent='空';d.classList.add('empty')}els.selectedRow.appendChild(d)}els.generate.disabled=state.selected.length!==3}
function renderDock(){els.dockList.replaceChildren();state.gimmicks.forEach(g=>{const m=META[g.template];const card=document.createElement('article');card.className='gimmick-card';if(state.focusGimmick===g.id||state.placing===g.id)card.classList.add('is-selected');card.innerHTML=`<span class="gimmick-name">${g.recipe.join('')} ${g.name}</span><div class="gimmick-meta">${m.icon} ${m.label} / ${g.placed?'配置済み':'未配置'}</div><div class="gimmick-help">押したままコートへドラッグすると設置できます</div>`;card.onpointerdown=(ev)=>startCardDrag(ev,g);els.dockList.appendChild(card)})}
function fit(){const r=canvas.getBoundingClientRect(),dpr=Math.min(2,devicePixelRatio||1),w=Math.floor(r.width*dpr),h=Math.floor(r.height*dpr);if(w>0&&h>0&&(canvas.width!==w||canvas.height!==h)){canvas.width=w;canvas.height=h}}
function fitCameraToGoals(){fit();const scale=canvas.width/WORLD.w;const viewH=canvas.height/scale;const ys=[START.y,...state.goals.map(g=>g.y)];const minY=Math.min(...ys)-120;const maxY=Math.max(...ys)+180;state.cameraY=clamp((minY+maxY-viewH)/2,-120,fallLine()-viewH+120);draw()}
function focusCameraOn(y){fit();const scale=canvas.width/WORLD.w;const viewH=canvas.height/scale;state.cameraY=clamp(y-viewH*.45,-120,fallLine()-viewH+120);draw()}
function draw(){if(!state)return;fit();const scale=canvas.width/WORLD.w,viewH=canvas.height/scale;state.cameraY=clamp(state.cameraY,-120,fallLine()-viewH+120);ctx.clearRect(0,0,canvas.width,canvas.height);ctx.save();ctx.scale(scale,scale);ctx.translate(0,-state.cameraY);drawField(viewH);state.fieldEmojis.forEach(drawEmoji);state.goals.forEach(drawGoal);state.ownGoals.forEach(drawOwn);state.gimmicks.filter(g=>g.placed).forEach(drawGimmick);drawBall();ctx.restore()}
function drawField(viewH){const top=state.cameraY-80,bottom=state.cameraY+viewH+80;ctx.fillStyle='#0f713e';ctx.fillRect(0,top,WORLD.w,bottom-top);ctx.strokeStyle='rgba(255,255,255,.14)';ctx.lineWidth=2;for(let y=Math.floor(top/80)*80;y<bottom;y+=80){ctx.beginPath();ctx.moveTo(0,y);ctx.lineTo(WORLD.w,y);ctx.stroke()}ctx.strokeStyle='rgba(232,255,89,.28)';ctx.lineWidth=5;round(18,top,WORLD.w-36,bottom-top,28);ctx.stroke();const f=fallLine();ctx.fillStyle='rgba(255,82,104,.16)';ctx.fillRect(0,f,WORLD.w,90);ctx.strokeStyle='#ff5268';ctx.setLineDash([18,12]);ctx.beginPath();ctx.moveTo(0,f);ctx.lineTo(WORLD.w,f);ctx.stroke();ctx.setLineDash([])}
function drawEmoji(e){ctx.save();ctx.translate(e.x,e.y);ctx.shadowColor='rgba(232,255,89,.7)';ctx.shadowBlur=14;ctx.font='31px system-ui';ctx.textAlign='center';ctx.textBaseline='middle';ctx.fillText(e.emoji,0,0);ctx.restore()}
function drawGoal(g){ctx.save();ctx.translate(g.x,g.y);ctx.globalAlpha=g.done?.45:1;ctx.strokeStyle=g.done?'#d5ddd5':'#e8ff59';ctx.fillStyle=g.done?'rgba(220,220,220,.08)':'rgba(232,255,89,.13)';ctx.lineWidth=9;round(-g.w/2,-g.h/2,g.w,g.h,18);ctx.fill();ctx.stroke();ctx.strokeStyle='rgba(255,255,255,.28)';ctx.lineWidth=2;for(let x=-g.w/2+18;x<g.w/2;x+=18){ctx.beginPath();ctx.moveTo(x,-g.h/2);ctx.lineTo(x,g.h/2);ctx.stroke()}ctx.fillStyle='#fff';ctx.font='900 18px system-ui';ctx.textAlign='center';ctx.fillText(g.done?'CLEAR':`GOAL ${g.label}`,0,-g.h/2-12);ctx.restore()}
function drawOwn(o){ctx.save();ctx.translate(o.x,o.y);ctx.fillStyle='rgba(255,82,104,.22)';ctx.strokeStyle='#ff5268';ctx.lineWidth=7;round(-o.w/2,-o.h/2,o.w,o.h,14);ctx.fill();ctx.stroke();ctx.fillStyle='#fff';ctx.font='900 24px system-ui';ctx.textAlign='center';ctx.fillText('☠️',0,8);ctx.restore()}
function drawGimmick(g){const m=META[g.template];ctx.save();ctx.translate(g.x,g.y);ctx.rotate(g.angle+(g.template==='ROTATE'?g.spin:0));ctx.strokeStyle=m.color;ctx.fillStyle=m.color+'33';ctx.lineWidth=6;if(['FLOW','ATTACH'].includes(g.template)){ctx.beginPath();ctx.arc(0,0,g.template==='ATTACH'?86:68,0,Math.PI*2);ctx.fill();ctx.stroke();arrow(58,m.color)}else if(g.template==='WARP'){ctx.beginPath();ctx.ellipse(0,0,34,48,0,0,Math.PI*2);ctx.fill();ctx.stroke()}else if(g.template==='CARRY'){round(-62,-18,124,36,18);ctx.fill();ctx.stroke()}else{ctx.beginPath();ctx.moveTo(-76,0);ctx.lineTo(76,0);ctx.stroke();if(['LAUNCH','SWITCH'].includes(g.template))arrow(72,m.color)}ctx.rotate(-g.angle-(g.template==='ROTATE'?g.spin:0));ctx.fillStyle='#fff';ctx.font='23px system-ui';ctx.textAlign='center';ctx.fillText(m.icon,0,-24);if(state.focusGimmick===g.id||state.placing===g.id)drawHandle(g);ctx.restore()}
function drawHandle(g){const hx=Math.cos(g.angle)*104,hy=Math.sin(g.angle)*104;ctx.save();ctx.strokeStyle='#e8ff59';ctx.lineWidth=4;ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(hx,hy);ctx.stroke();ctx.fillStyle='#e8ff59';ctx.beginPath();ctx.arc(hx,hy,18,0,Math.PI*2);ctx.fill();ctx.fillStyle='#06150e';ctx.font='900 13px system-ui';ctx.textAlign='center';ctx.textBaseline='middle';ctx.fillText('回転',hx,hy);ctx.restore()}
function arrow(l,c){ctx.strokeStyle=c;ctx.fillStyle=c;ctx.lineWidth=5;ctx.beginPath();ctx.moveTo(-l/2,0);ctx.lineTo(l/2,0);ctx.stroke();ctx.beginPath();ctx.moveTo(l/2+7,0);ctx.lineTo(l/2-10,-9);ctx.lineTo(l/2-10,9);ctx.closePath();ctx.fill()}
function drawBall(){const b=state.ball;ctx.save();ctx.translate(b.x,b.y);ctx.fillStyle='#fff';ctx.strokeStyle='#111';ctx.lineWidth=4;ctx.beginPath();ctx.arc(0,0,b.r,0,Math.PI*2);ctx.fill();ctx.stroke();ctx.font='19px system-ui';ctx.textAlign='center';ctx.textBaseline='middle';ctx.fillText('⚽',0,1);ctx.restore()}
function round(x,y,w,h,r){ctx.beginPath();ctx.moveTo(x+r,y);ctx.lineTo(x+w-r,y);ctx.quadraticCurveTo(x+w,y,x+w,y+r);ctx.lineTo(x+w,y+h-r);ctx.quadraticCurveTo(x+w,y+h,x+w-r,y+h);ctx.lineTo(x+r,y+h);ctx.quadraticCurveTo(x,y+h,x,y+h-r);ctx.lineTo(x,y+r);ctx.quadraticCurveTo(x,y,x+r,y)}
function worldFromEvent(e){const r=canvas.getBoundingClientRect(),scale=canvas.width/WORLD.w;return {x:(e.clientX-r.left)*(canvas.width/r.width)/scale,y:(e.clientY-r.top)*(canvas.height/r.height)/scale+state.cameraY,inside:e.clientX>=r.left&&e.clientX<=r.right&&e.clientY>=r.top&&e.clientY<=r.bottom}}
function hitGimmick(p){return [...state.gimmicks].reverse().find(g=>g.placed&&Math.hypot(g.x-p.x,g.y-p.y)<85)}
function handlePos(g){return {x:g.x+Math.cos(g.angle)*104,y:g.y+Math.sin(g.angle)*104}}
function startCardDrag(ev,g){ev.preventDefault();state.placing=g.id;state.focusGimmick=g.id;drag={type:'new',id:g.id};showHelp('そのままコートへドラッグすると設置できます。');render()}
function onDown(ev){if(state.phase==='run'||state.phase==='between'||state.gameover)return;const p=worldFromEvent(ev);const h=[...state.gimmicks].reverse().find(g=>g.placed&&Math.hypot(handlePos(g).x-p.x,handlePos(g).y-p.y)<34);if(h){state.focusGimmick=h.id;drag={type:'rotate',id:h.id};showHelp('黄色い「回転」ハンドルで角度を変えられます。');return}const hit=hitGimmick(p);if(hit){state.focusGimmick=hit.id;drag={type:'move',id:hit.id,dx:hit.x-p.x,dy:hit.y-p.y};showHelp('ドラッグで移動。黄色い「回転」ハンドルで角度変更。');render();draw();return}if(state.placing){const g=state.gimmicks.find(x=>x.id===state.placing);if(g){g.x=p.x;g.y=p.y;g.placed=true;state.focusGimmick=g.id;drag={type:'move',id:g.id,dx:0,dy:0};render();draw();return}}drag={type:'pan',startY:ev.clientY,camera:state.cameraY}}
function onMove(ev){if(!drag)return;ev.preventDefault();const p=worldFromEvent(ev);const g=state.gimmicks.find(x=>x.id===drag.id);if(drag.type==='new'&&p.inside&&g){g.x=p.x;g.y=p.y;g.placed=true;state.focusGimmick=g.id;draw();return}if(drag.type==='move'&&g){g.x=clamp(p.x+(drag.dx||0),40,WORLD.w-40);g.y=p.y+(drag.dy||0);draw();return}if(drag.type==='rotate'&&g){g.angle=Math.atan2(p.y-g.y,p.x-g.x);draw();return}if(drag.type==='pan'){state.cameraY=drag.camera-(ev.clientY-drag.startY)/(canvas.width/WORLD.w);draw()}}
function onUp(){if(drag){const g=state.gimmicks.find(x=>x.id===drag.id);if(g&&g.placed){state.placing=null;state.focusGimmick=g.id;hideHelp();setCoach('配置完了','キックオフ中でなければ、いつでもギミックを移動・回転できます。準備できたらキックオフ。');if(state.tutorial==='place'){showModal('角度も変えられる','編集もサッカーのうち','置いたギミックに出ている黄色い「回転」ハンドルをドラッグすると角度が変わります。調整したらキックオフ！','了解',()=>closeModal());state.tutorial='edit'}}drag=null;render();draw()}}
els.modalPrimary.onclick=()=>{if(modalAction)modalAction()};els.modalSecondary.onclick=closeModal;els.kick.onclick=startRun;els.center.onclick=fitCameraToGoals;els.generate.onclick=generateGimmick;document.querySelectorAll('.tab-btn').forEach(b=>b.onclick=()=>setTab(b.dataset.tab));canvas.addEventListener('pointerdown',onDown,{passive:false});window.addEventListener('pointermove',onMove,{passive:false});window.addEventListener('pointerup',onUp);window.addEventListener('resize',fitCameraToGoals);boot();
