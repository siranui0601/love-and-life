# anti trap normalizer

Generated field gimmicks can trap the ball when lift, slow, orbit, vortex, or tether behavior has no outgoing push.

This branch adds guard motors for risky generated gimmicks:

- current toward the next goal
- speed floor toward the next goal

The same guard also runs in a small client fetch normalizer before the game receives generated JSON.
